\clearpage

\section{基于双重注意力机制的 OCT 图像语义分割方法}
\markboth{第四章\hspace{1em}基于双重注意力机制的 OCT 图像语义分割方法}{}

OCT 图像存在散斑噪声强、对比度低、边界模糊等特点，给熔深区域的自动化提取带来困难。本章以 DeepLabV3+ 为基线模型，针对 ASPP 模块全局上下文建模不足和解码器边界定位不精确两个问题，分别在编码器端和解码器端引入 TR（Transformer Routing）全局注意力模块和 SAE（Spatial Awareness Enhancement）空间感知增强模块，通过消融实验和对比实验验证所提方法的有效性。

\subsection{DeepLabV3+ 基线模型}

DeepLabV3+ 由 Google 团队于 2018 年提出\upcite{Chen2018DeepLab}，结合空洞卷积、ASPP 模块和编码器-解码器结构，在多尺度上下文信息捕获方面表现出色。DeepLabV3+ 的整体网络结构已在第二章图~\ref{fig:deeplabv3plus_structure}~中详细展示。

\subsubsection{空洞卷积}

空洞卷积（Atrous Convolution）通过在卷积核的相邻元素之间插入空洞（零值），在不增加计算量的前提下扩大感受野。空洞卷积的示意图如图~\ref{fig:dilated_conv}~所示。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth]{figure/cp4/fig4-1_dilated_conv.png}
\caption{空洞卷积示意图\upcite{Patel2021Atrous}}
\label{fig:dilated_conv}
\end{figure}

设输入特征图为 $\mathbf{F}$，空洞卷积核为 $\mathbf{W}$，空洞率为 $r$，则空洞卷积的输出可表示为：
\begin{equation}
\mathbf{O}[i, j] = \sum_{m} \sum_{n} \mathbf{F}[i + r \cdot m, j + r \cdot n] \cdot \mathbf{W}[m, n]
\label{eq:dilated_conv}
\end{equation}
其中，$(i, j)$ 表示输出特征图的位置坐标，$(m, n)$ 表示卷积核的索引。当 $r = 1$ 时退化为标准卷积；当 $r > 1$ 时，有效感受野扩大为 $(2r - 1) \times k + 1$，$k$ 为卷积核原始尺寸。

\subsubsection{ASPP 模块}

ASPP（Atrous Spatial Pyramid Pooling）模块通过多个不同空洞率的并行分支捕获多尺度上下文信息\upcite{Chen2017DeepLabV3}，结构如图~\ref{fig:aspp_module}~所示。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figure/cp4/fig4-2b_aspp_module.png}
\caption{ASPP模块结构图}
\label{fig:aspp_module}
\end{figure}

本文采用深度可分离 ASPP 模块，包含 5 个并行分支：1×1 卷积、3×3 空洞卷积（dilation 分别为 12、24、36）和全局平均池化。5 个分支的输出在通道维度拼接后，通过 1×1 卷积降维融合。深度可分离卷积将标准卷积分解为深度卷积和点卷积两步，参数量和计算量大幅降低。

\subsubsection{编码器-解码器结构}

编码器采用 ResNet18-V1c 作为骨干网络\upcite{He2015ResNet,wu2016widerdeeper}，使用 Deep Stem 结构（三个 3×3 卷积替代 7×7 卷积）。选择 ResNet18 而非更深的 ResNet50/101，主要考虑训练集规模有限（912 张图像），过深网络在小数据集上易过拟合，且 OCT 图像为二分类任务，浅层网络已具备足够的特征提取能力。

解码器通过上采样和特征融合恢复空间分辨率：ASPP 输出的高层特征经双线性插值上采样，与编码器浅层特征（经 1×1 卷积降维后）在通道维度拼接，再经深度可分离卷积和 1×1 卷积输出像素级预测结果。

\subsection{TR 全局注意力模块设计}

\subsubsection{改进动机}

ASPP 模块的感受野受限于空洞卷积的局部性，仅覆盖特征图的局部区域。而 OCT 图像中的熔深区域呈现细长结构（纵横比可达 1:10 以上），跨越图像多个区域，需要网络理解整个图像的空间关系才能准确分割。ASPP 的局部感受野可能导致细长结构的断裂或误分割。

表~\ref{tab:oct_module_relation}~总结了 OCT 图像特点与本文模块设计的对应关系。

\begin{table}[htbp]
\centering
\caption{OCT图像特点与模块设计的对应关系}
\label{tab:oct_module_relation}
\begin{tabular}{p{2.5cm}p{4.5cm}p{4.5cm}}
\toprule
\textbf{OCT图像特点} & \textbf{对分割任务的影响} & \textbf{本文解决方案} \\
\midrule
散斑噪声强 & 局部特征不稳定，影响边界识别 & TR模块的区域级路由在窗口平均后计算相似度，抑制局部噪声干扰 \\
\midrule
目标细长 & 纵横比高，跨越多区域，易断裂 & TR模块的长程依赖建模使网络理解细长结构的整体连续性 \\
\midrule
边界模糊 & 对比度低，边界定位困难 & SAE模块的坐标注意力显式编码空间位置，增强边界特征响应 \\
\midrule
类别不平衡 & 背景占比90\%以上，前景学习不充分 & BCE+Dice混合损失函数 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{TR 模块的整体结构}

TR 模块受 BiFormer\upcite{zhu2023biformervisiontransformerbilevel} 启发，采用 Transformer Block 架构，整体结构如图~\ref{fig:tr_module}~所示。模块接收 ASPP 的拼接输出特征图，经以下三个子模块依次处理：

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{figure/cp4/fig4-3_tr_module.png}
\caption{TR模块结构图（窗口大小 S=4，窗口数量 S\textsuperscript{2}=16，TopK=4）}
\label{fig:tr_module}
\end{figure}

（1）卷积位置编码（Convolutional Position Encoding, CPE）。使用 $3 \times 3$ 深度可分离卷积为特征图注入隐式位置信息，输出与输入相加，使模型在不依赖固定位置编码的情况下感知空间位置。

（2）双层路由注意力（Bi-level Routing Attention, BRA）。对 LayerNorm 归一化后的特征执行双层路由稀疏注意力（详见下文），输出通过残差连接与输入相加。

（3）前馈网络（Feed-Forward Network, FFN）。对 LayerNorm 归一化后的特征依次通过线性层（升维 4 倍）、深度可分离卷积、GELU 激活和线性层（降维），输出同样通过残差连接。深度可分离卷积的引入使 FFN 在通道混合的同时保留局部空间信息。

整个模块的前向传播可表示为：
\begin{align}
\mathbf{X}_1 &= \mathbf{X}_0 + \text{CPE}(\mathbf{X}_0) \\
\mathbf{X}_2 &= \mathbf{X}_1 + \text{DropPath}\!\left(\text{BRA}\!\left(\text{LN}(\mathbf{X}_1)\right)\right) \\
\mathbf{X}_3 &= \mathbf{X}_2 + \text{DropPath}\!\left(\text{FFN}\!\left(\text{LN}(\mathbf{X}_2)\right)\right)
\end{align}
其中 DropPath 以一定概率随机丢弃整条路径，起到正则化作用。

\subsubsection{双层路由注意力机制}

双层路由注意力（Bi-Level Routing Attention, BRA）是 TR 模块的核心计算单元，通过区域级路由和 TopK 选择机制，仅对最相关的窗口进行注意力计算，将计算复杂度从全局注意力的 $O(n^2)$ 降低到 $O(n^2 \times k/p^2)$。其工作流程如图~\ref{fig:bilevel_attention}~所示，包含以下四个步骤。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figure/cp4/fig4-4_bilevel_attention.png}
\caption{双层路由注意力机制示意图}
\label{fig:bilevel_attention}
\end{figure}

\textbf{步骤一，QKV 投影与窗口划分。}输入特征图通过线性层投影为查询 $\mathbf{Q}$、键 $\mathbf{K}$ 和值 $\mathbf{V}$，然后按 $n_{\text{win}} \times n_{\text{win}}$ 的网格划分为 $p^2$ 个窗口，每个窗口包含 $w \times w$ 个像素。同时，值 $\mathbf{V}$ 经过一个 $5 \times 5$ 深度可分离卷积生成局部上下文增强（Local Context Enhancement, LCE）特征，用于在注意力输出后补充局部细节。

\textbf{步骤二，区域级路由。}对每个窗口内的 $\mathbf{Q}$ 和 $\mathbf{K}$ 分别求空间平均，得到区域级查询和键向量：
\begin{equation}
\mathbf{Q}_{\text{region}}^{(i)} = \frac{1}{|\mathcal{W}_i|} \sum_{j \in \mathcal{W}_i} \mathbf{Q}_j, \quad \mathbf{K}_{\text{region}}^{(i)} = \frac{1}{|\mathcal{W}_i|} \sum_{j \in \mathcal{W}_i} \mathbf{K}_j
\label{eq:region_qk}
\end{equation}
计算窗口间的路由相似度矩阵 $\mathbf{S} \in \mathbb{R}^{p^2 \times p^2}$，并对每个查询窗口选择 TopK 个最相关的键窗口：
\begin{equation}
\mathbf{S}_{ij} = \frac{\mathbf{Q}_{\text{region}}^{(i)} \cdot (\mathbf{K}_{\text{region}}^{(j)})^T}{\sqrt{d_k}}, \quad \mathcal{R}_i = \text{TopK}(\mathbf{S}_{i,:},\, k)
\label{eq:routing}
\end{equation}

\textbf{步骤三，KV 聚集。}根据路由索引 $\mathcal{R}_i$，从全部窗口中收集被选中的键值对，构成稀疏的 KV 集合 $\mathbf{K}_{\mathcal{R}_i}$、$\mathbf{V}_{\mathcal{R}_i}$。未被选中的窗口不参与后续计算，从而减少了注意力矩阵的规模。

\textbf{步骤四，多头稀疏注意力。}在每个查询窗口内，对聚集后的 KV 执行标准的缩放点积多头注意力：
\begin{equation}
\text{Attention}(\mathbf{Q}_i, \mathbf{K}_{\mathcal{R}_i}, \mathbf{V}_{\mathcal{R}_i}) = \text{softmax}\!\left(\frac{\mathbf{Q}_i \mathbf{K}_{\mathcal{R}_i}^T}{\sqrt{d_k}}\right) \mathbf{V}_{\mathcal{R}_i}
\label{eq:fine_attention}
\end{equation}
注意力输出与 LCE 特征相加后，经线性投影恢复到原始维度。

\subsubsection{TR 模块与 BiFormer 的差异}

本文的 TR 模块与原始 BiFormer 的主要区别在于嵌入位置和参数选择。BiFormer 作为独立的骨干网络使用，而本文将 TR 模块嵌入 ASPP 输出之后，利用 ASPP 已捕获的多尺度特征作为输入，使注意力机制在多尺度语义信息的基础上建立长程依赖。此外，区域级路由中的窗口平均操作对散斑噪声有天然的平滑作用，使路由决策在 OCT 图像的噪声条件下仍能保持稳定。TopK 取 4，窗口大小取 $4 \times 4$，注意力头数取 8，均沿用 BiFormer 在语义分割任务上的推荐配置。

\subsubsection{混合损失函数}

OCT 图像中背景占 90\% 以上像素，单一的交叉熵损失容易被背景像素主导。本文采用 BCE 与 Dice 的混合损失：
\begin{equation}
L_{\text{BCE}} = -\frac{1}{N} \sum_{i=1}^{N} \left[ y_i \log(\sigma(p_i)) + (1-y_i) \log(1-\sigma(p_i)) \right]
\label{eq:bce_loss}
\end{equation}
\begin{equation}
L_{\text{Dice}} = 1 - \frac{2 \sum_{i=1}^{N} \sigma(p_i) \cdot y_i + \epsilon}{\sum_{i=1}^{N} \sigma(p_i) + \sum_{i=1}^{N} y_i + \epsilon}
\label{eq:dice_loss}
\end{equation}
\begin{equation}
L_{\text{total}} = \lambda_1 \cdot L_{\text{BCE}} + \lambda_2 \cdot L_{\text{Dice}}
\label{eq:total_loss}
\end{equation}
其中 BCE 损失提供稳定的逐像素梯度，Dice 损失直接优化区域重叠度、对前景占比不敏感。

\textbf{权重选取依据。}本文未对 $\lambda_1$、$\lambda_2$ 进行精细网格搜索，而是基于"两类损失各承担一部分梯度信号"的原则在等比与不等比之间做了简单对照。考虑到本文采用 Adam 优化器、初始学习率仅为 $1\times 10^{-6}$，等比权重 $\lambda_1=\lambda_2$ 可以避免 BCE 或 Dice 单方主导前期梯度，整体放大 2 倍则用于在低学习率下加快收敛、缩小目标类与背景类的梯度量级差。为验证该选取的合理性，在改进模型（DeepLabV3+ + TR + SAE）上对若干组权重做了 mIoU 对照，结果如表~\ref{tab:loss_weight}~所示。

\begin{table}[htbp]
\centering
\renewcommand\arraystretch{1.4}
\caption{BCE/Dice 混合损失权重对照（改进模型）}
\label{tab:loss_weight}
\begin{tabular}{cccc}
\toprule
$(\lambda_1, \lambda_2)$ & 权重特点 & mIoU & HD95 \\
\midrule
(1, 1) & BCE/Dice 等权、未放大 & 0.903 & 11.42 \\
(1, 2) & 偏向 Dice、强化区域重叠 & 0.905 & 11.18 \\
(2, 1) & 偏向 BCE、强化像素监督 & 0.902 & 11.36 \\
\textbf{(2, 2)} & \textbf{等权、整体放大 2 倍（本文）} & \textbf{0.911} & \textbf{10.68} \\
(2, 3) & 等权基础上略偏 Dice & 0.906 & 10.97 \\
(3, 3) & 等权、整体放大 3 倍 & 0.908 & 10.84 \\
\bottomrule
\end{tabular}
\end{table}

由表~\ref{tab:loss_weight}~可知，当 $\lambda_1=\lambda_2$ 时模型整体表现优于不等比配置；整体放大倍数取 2 时 mIoU 与 HD95 同时取得最优，再继续放大到 3 倍后边界精度有所回落，说明过大的损失幅值在小学习率下并不会带来额外收益。综合精度与边界指标，本文选用 $\lambda_1=\lambda_2=2.0$ 作为最终配置。

\subsection{SAE 空间感知增强模块设计}

\subsubsection{改进动机}

TR 模块在编码器端增强了全局结构理解，但解码器在恢复空间分辨率时仅通过简单的特征拼接融合高低层信息，缺乏对空间位置关系的显式建模。编码器端的全局信息难以传递至解码器的边界细节恢复环节，导致边界轮廓模糊、局部不连续。

传统解码器存在三个局限：多次下采样导致空间位置信息丢失，恢复有限；所有通道均匀加权，未区分边界通道与非边界通道的贡献差异；缺乏对细长结构在水平和垂直方向上特征分布差异的方向性建模。

\subsubsection{SAE 模块原理与结构}

SAE 模块在坐标注意力（Coordinate Attention, CA）\upcite{hou2021coordinateattentionefficientmobile}与 SE 通道注意力\upcite{hu2019squeezeandexcitationnetworks}的基础上进行了针对性架构设计，采用"坐标注意力→特征精炼→多基数通道注意力"的三级级联结构，如图~\ref{fig:sae_structure}~所示。模块接收解码器中低层特征与高层特征拼接后的融合特征，每级输出均通过残差连接与输入相加。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{figure/cp4/fig4-5_sae_module.png}
\caption{SAE 模块结构图}
\label{fig:sae_structure}
\end{figure}

\textbf{第一级：坐标注意力（Coordinate Attention）。}将全局池化解耦为两个方向的一维聚合。给定输入 $\mathbf{X} \in \mathbb{R}^{C \times H \times W}$，分别沿水平和垂直方向池化：
\begin{equation}
z_c^h(h) = \frac{1}{W}\sum_{0 \leq i < W} x_c(h, i), \quad z_c^w(w) = \frac{1}{H}\sum_{0 \leq j < H} x_c(j, w)
\end{equation}
得到 $\mathbf{z}^h \in \mathbb{R}^{C \times H \times 1}$ 和 $\mathbf{z}^w \in \mathbb{R}^{C \times 1 \times W}$，分别保留了水平和垂直方向的位置信息。将二者沿空间维度拼接后，通过共享 $1 \times 1$ 卷积降维（降维比 $r = 4$）、BN 和 h-swish 激活：
\begin{equation}
\mathbf{f} = \text{h-swish}\!\left(\text{BN}\!\left(F_{1 \times 1}([\mathbf{z}^h;\, \mathbf{z}^w])\right)\right)
\end{equation}
再沿空间维度切分，分别通过独立的 $1 \times 1$ 卷积和 Sigmoid 激活生成方向性注意力权重 $\mathbf{g}^h \in \mathbb{R}^{C \times H \times 1}$、$\mathbf{g}^w \in \mathbb{R}^{C \times 1 \times W}$，对输入特征图进行空间重标定：
\begin{equation}
\mathbf{Y}_1 = \mathbf{X} \odot \mathbf{g}^h \odot \mathbf{g}^w + \mathbf{X}
\end{equation}
其中 $\odot$ 表示逐元素乘法（沿对应维度广播）。h-swish 激活函数定义为 $\text{h-swish}(x) = x \cdot \text{ReLU6}(x+3)/6$，相比标准 Sigmoid 计算效率更高。

\textbf{第二级：特征精炼。}坐标注意力的输出经过两层 $3 \times 3$ 卷积（每层后接 BN 和 ReLU），融合邻域上下文信息，输出通过残差连接：
\begin{equation}
\mathbf{Y}_2 = \text{Conv}_{3 \times 3}\!\left(\text{Conv}_{3 \times 3}(\mathbf{Y}_1)\right) + \mathbf{Y}_1
\end{equation}
该级的作用是将坐标注意力增强后的特征与局部空间上下文进一步融合，弥补一维池化丢失的局部细节。

\textbf{第三级：多基数通道注意力（Multi-Cardinality SE）。}通过全局平均池化（Global Average Pooling, GAP）将空间维度压缩为 $1 \times 1$，然后使用 $\kappa$ 个并行的全连接分支，每个分支独立将通道维度从 $C$ 降至 $C/r$，经 ReLU 激活后拼接，再通过一个全连接层和 Sigmoid 恢复到 $C$ 维，生成通道权重：
\begin{equation}
\mathbf{s} = \sigma\!\left(W_{\text{fc}}\!\left[\text{ReLU}(W_1 \mathbf{p});\, \text{ReLU}(W_2 \mathbf{p});\, \text{ReLU}(W_3 \mathbf{p});\, \text{ReLU}(W_4 \mathbf{p})\right]\right)
\end{equation}
其中 $\mathbf{p} = \text{GAP}(\mathbf{Y}_2) \in \mathbb{R}^C$，$W_i \in \mathbb{R}^{(C/r) \times C}$ 为第 $i$ 个分支的权重矩阵，$W_{\text{fc}} \in \mathbb{R}^{C \times (\kappa \cdot C/r)}$ 为融合层权重，本文取 $\kappa = 4$，$r = 4$。多个并行分支各自学习不同的通道依赖模式，相比单一 SE 路径提供了更丰富的通道间特征交互。最终输出为：
\begin{equation}
\mathbf{Y}_3 = \mathbf{Y}_2 \odot \mathbf{s}
\end{equation}

SAE 模块集成于 DeepLabV3+ 解码器中低层特征与高层特征拼接之后。该位置的特征同时包含浅层的空间细节和深层的语义信息，在此处引入空间感知增强可以在特征融合阶段突出边界相关响应。模块输出通过残差连接与输入相加，每级均保持梯度流通。

\subsection{改进模型整体结构与协同机制}

\subsubsection{完整模型架构}

改进后的模型整体结构如图~\ref{fig:final_model}~所示，在 DeepLabV3+ 基础上做了三项优化：编码器端在 ASPP 之后引入 TR 模块建立长距离依赖；解码器端在低层特征处理分支中集成 SAE 模块增强空间位置感知；训练时采用 BCE+Dice 混合损失函数缓解类别不平衡。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{figure/cp4/fig4-2_improved_model_structure.png}
\caption{改进模型（DeepLabV3+ + TR + SAE）整体结构图}
\label{fig:final_model}
\end{figure}

\subsubsection{各模块协同机制与前向传播流程}

改进模型的完整前向传播流程如下。输入图像经 ResNet18 编码器提取四级特征，其中第四级特征（$C = 512$）送入 ASPP 模块，5 个分支的输出在通道维度拼接后得到 $C = 2560$ 的特征图。TR 模块在此特征上执行双层路由注意力，通过残差连接输出增强后的全局特征，再经 $1 \times 1$ 瓶颈卷积降维至 $C = 512$。该特征经双线性插值上采样后，与编码器第一级浅层特征（经 $1 \times 1$ 卷积降维至 $C = 48$）在通道维度拼接，得到 $C = 560$ 的融合特征。SAE 模块在此融合特征上依次执行坐标注意力、特征精炼和多基数通道注意力，通过残差连接输出空间增强后的特征。最终经深度可分离卷积和 $1 \times 1$ 分类卷积输出像素级预测。

TR 模块位于编码器端，在高层语义特征上建立长距离依赖，解决细长结构的断裂分割问题；SAE 模块位于解码器端，在融合特征上增强空间位置感知，提升边界定位精度。二者形成"编码端重结构、解码端重细节"的互补架构。消融实验的数据直观地反映了这种互补性：单独使用 TR 可提升 mIoU 3.9\%，单独使用 SAE 可提升 5.9\%，同时使用可提升 7.1\%。

\subsection{综合实验与分析}

\subsubsection{实验环境与参数配置}

实验环境与训练参数配置如表~\ref{tab:env_train_config}~所示。

\begin{table}[htbp]
\centering
\caption{实验环境与训练参数配置}
\label{tab:env_train_config}
\begin{tabular}{cc}
\toprule
配置项 & 参数 \\
\midrule
GPU & NVIDIA GeForce RTX 4090D (24GB) × 1 \\
CPU & AMD EPYC 9754 128-Core Processor（18 vCPU） \\
内存 & 60 GB \\
操作系统 & Ubuntu 20.04 LTS \\
深度学习框架 & PyTorch 1.10.0 / MMSegmentation \\
CUDA & 11.3 \\
\midrule
训练迭代次数 & 20,000 次 \\
批量大小 & 8 \\
优化器 & Adam（初始学习率 $1 \times 10^{-6}$） \\
学习率调度 & 多项式衰减（power=0.9） \\
输入尺寸 & $512 \times 512$ \\
评估间隔 & 每 500 次迭代 \\
\bottomrule
\end{tabular}
\end{table}

评价指标包括 mIoU（平均交并比）、mAcc（平均准确率）、mDice（平均 Dice 系数）和 HD95（Hausdorff 距离 95\% 分位数）。

\subsubsection{消融实验}

表~\ref{tab:ablation}~给出了 4 种配置的消融实验结果。

\begin{table}[htbp]
\centering
\caption{消融实验结果}
\label{tab:ablation}
\begin{tabular}{lcccccc}
\toprule
配置 & TR模块 & SAE模块 & mIoU & mAcc & mDice & 相比基线提升 \\
\midrule
DeepLabV3+ (基线) & $\times$ & $\times$ & 0.851 & 0.923 & 0.913 & - \\
+ TR & $\checkmark$ & $\times$ & 0.884 & 0.941 & 0.935 & +3.9\% \\
+ SAE & $\times$ & $\checkmark$ & 0.901 & 0.948 & 0.945 & +5.9\% \\
+ TR + SAE & $\checkmark$ & $\checkmark$ & 0.911 & 0.956 & 0.951 & +7.1\% \\
\bottomrule
\end{tabular}
\end{table}

TR 模块通过全局依赖建模使 mIoU 从 0.851 提升至 0.884（+3.9\%），SAE 模块仅增加约 25M 参数量即实现 mIoU 5.9\% 的提升，两模块同时使用达到 0.911（+7.1\%）。组合提升幅度（7.1\%）小于单模块之和（9.8\%），说明二者在提升路径上有一定重叠，但组合配置在所有指标上均为最优，体现了全局与局部增强的互补性。

表~\ref{tab:boundary}~给出了边界精度对比。

\begin{table}[htbp]
\centering
\caption{消融实验边界精度对比}
\label{tab:boundary}
\begin{tabular}{lcc}
\toprule
模型配置 & HD95 & 边界精度评价 \\
\midrule
DeepLabV3+ (基线) & 12.22 & 良好 \\
DeepLabV3+ + TR & 11.89 & 良好 \\
DeepLabV3+ + SAE & 11.45 & 优秀 \\
本文方法 & 10.68 & 优秀 \\
\bottomrule
\end{tabular}
\end{table}

SAE 模块对边界精度的贡献更突出（HD95 从 12.22 降至 11.45），结合 TR 后进一步降至 10.68，相比基线降低 12.6\%。

\subsubsection{与主流方法对比实验}

表~\ref{tab:comparison}~给出了定量对比结果，表~\ref{tab:class_performance}~给出了类别级别的性能对比。

\begin{table}[htbp]
\centering
\caption{不同方法的定量评估结果对比}
\label{tab:comparison}
\begin{tabular}{lccccc}
\toprule
模型配置 & mIoU & mAcc & mDice & mPrecision & mRecall \\
\midrule
UNet & 0.805 & 0.878 & 0.880 & 0.883 & 0.878 \\
UNet++ & 0.751 & 0.905 & 0.837 & 0.790 & 0.905 \\
ResUNet & 0.721 & 0.788 & 0.810 & 0.836 & 0.788 \\
TransUNet & 0.810 & 0.963 & 0.884 & 0.829 & 0.963 \\
DeepLabV3+ (基线) & 0.851 & 0.923 & 0.913 & 0.904 & 0.923 \\
本文方法 & 0.911 & 0.956 & 0.951 & 0.947 & 0.956 \\
\bottomrule
\end{tabular}
\end{table}

本文方法 mIoU 达到 0.911，相比 UNet 提升 13.2\%，相比 TransUNet 提升 12.5\%，相比基线提升 7.1\%。TransUNet 在 mAcc 上略高（0.963 vs 0.956），但其 mIoU 仅 0.810，且内存占用远大于本文方法（详见效率分析）。

\begin{table}[htbp]
\centering
\caption{不同方法在类别级别上的性能对比}
\label{tab:class_performance}
\begin{tabular}{lccccc}
\toprule
\multirow{2}{*}{模型配置} & \multicolumn{2}{c}{背景类别} & \multicolumn{3}{c}{目标类别} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-6}
 & IoU & Acc & IoU & Acc & Dice \\
\midrule
UNet & 0.990 & 0.995 & 0.620 & 0.761 & 0.765 \\
UNet++ & 0.983 & 0.987 & 0.518 & 0.823 & 0.683 \\
ResUNet & 0.985 & 0.994 & 0.457 & 0.581 & 0.627 \\
TransUNet & 0.988 & 0.999 & 0.632 & 0.926 & 0.773 \\
DeepLabV3+ (基线) & 0.992 & 0.996 & 0.710 & 0.850 & 0.830 \\
本文方法 & 0.996 & 0.998 & 0.826 & 0.914 & 0.904 \\
\bottomrule
\end{tabular}
\end{table}

目标类别 IoU 达到 0.826，相比基线（0.710）提升 16.3\%，相比 UNet（0.620）提升 33.2\%。这一差距主要来自于 TR 模块避免了细长结构断裂、SAE 模块提高了边界定位精度。

\subsubsection{可视化分析}

图~\ref{fig:comparison_all}~展示了全部方法的分割结果可视化对比。

\begin{figure}[htbp]
\centering
\subfigure[原图]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/origin.png}
}
\subfigure[标签]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/label.png}
}
\subfigure[UNet]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/unet.png}
}
\subfigure[UNet++]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/unetpp.png}
}
\subfigure[ResUNet]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/resunet.png}
}
\\
\subfigure[TransUNet]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/transunet.png}
}
\subfigure[DeepLabV3+]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/v3plus.png}
}
\subfigure[+TR]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/tr.png}
}
\subfigure[+SAE]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/sae.png}
}
\subfigure[+TR+SAE]{
    \includegraphics[width=0.18\textwidth]{figure/cp4/4-3/all.png}
}
\caption{全部方法的分割结果可视化对比}
\label{fig:comparison_all}
\end{figure}

从可视化结果可以看出：UNet 系列在细长结构上出现断裂，TransUNet 有所改善但精度不足；单独加 TR 后连续性改善，单独加 SAE 后边界更锐利，两者同时使用时分割结果兼具全局完整性和边界精确性。

\subsubsection{效率分析}

表~\ref{tab:efficiency}~从参数量和内存占用两个方面对不同方法进行对比分析。

\begin{table}[htbp]
\centering
\caption{不同方法的效率指标对比}
\label{tab:efficiency}
\begin{tabular}{lcc}
\toprule
模型配置 & 参数量(M) & 内存占用(MB) \\
\midrule
UNet & 30 & 3809 \\
UNet++ & 8.8 & 4675 \\
ResUNet & 13 & 4351 \\
TransUNet & 219 & 19530 \\
DeepLabV3+ (基线) & 95 & 2705 \\
本文方法 & 420 & 9107 \\
\bottomrule
\end{tabular}
\end{table}

本文方法参数量 420M，相比基线增加 342.1\%，但 mIoU 提升 7.1\%。与 TransUNet 相比，mIoU 高 12.5\% 而内存占用低 53.3\%（9107MB vs 19530MB），这得益于 TR 模块的双层路由稀疏注意力机制减少了中间激活值的存储需求。

\subsubsection{散斑强度鲁棒性对照实验}

OCT 图像最主要的噪声形式为散斑噪声，其强度随被测样品微结构、成像景深与系统参数变化而波动。为进一步评估改进模型在不同散斑强度下的稳定性，本文在测试集 228 张图像上人工叠加乘性散斑噪声，模拟从"原始 / 轻度 / 中度 / 重度"四档干扰条件。具体地，给定原始图像 $I$，按下式生成扰动图像：
\begin{equation}
\tilde{I} = I \cdot (1 + n), \quad n \sim \mathcal{N}(0, \sigma^2)
\end{equation}
其中乘性高斯随机场 $n$ 近似 OCT 散斑的瑞利包络统计，方差 $\sigma^2 \in \{0,\ 0.02,\ 0.05,\ 0.10\}$ 分别对应四档强度。基线 DeepLabV3+ 与本文方法（DeepLabV3+ + TR + SAE）使用同一组训练权重直接在扰动测试集上进行推理，不做额外微调，结果如表~\ref{tab:robust_speckle}~所示。

\begin{table}[htbp]
\centering
\renewcommand\arraystretch{1.4}
\caption{不同散斑强度下基线与本文方法的鲁棒性对照}
\label{tab:robust_speckle}
\begin{tabular}{ccccc}
\toprule
\multirow{2}{*}{$\sigma^2$} & \multicolumn{2}{c}{基线（DeepLabV3+）} & \multicolumn{2}{c}{本文方法} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
 & mIoU & 衰减幅度 & mIoU & 衰减幅度 \\
\midrule
0     & 0.851 & ---     & 0.911 & ---     \\
0.02  & 0.838 & $-$1.5\% & 0.903 & $-$0.9\% \\
0.05  & 0.806 & $-$5.3\% & 0.886 & $-$2.7\% \\
0.10  & 0.762 & $-$10.5\% & 0.857 & $-$5.9\% \\
\bottomrule
\end{tabular}
\end{table}

由表~\ref{tab:robust_speckle}~可见，随散斑强度增大，两模型 mIoU 均出现单调下降，但本文方法在各档强度下的相对衰减幅度均明显小于基线，并保持约一半左右的衰减率（如 $\sigma^2=0.10$ 时基线衰减 10.5\%、本文方法仅衰减 5.9\%）。原因在于：TR 模块在区域级路由阶段对窗口内的 $\mathbf{Q}$、$\mathbf{K}$ 做空间平均，相当于对局部散斑做了天然的低通滤波，使得长程依赖建模不易被局部噪声扰动；SAE 模块沿水平/垂直方向的一维池化与坐标注意力则在解码端继续抑制散斑随机颗粒对边界响应的干扰。综上，所提双重注意力机制对散斑干扰具备明显的抗噪能力。

\subsection{本章小结}

本章针对焊接 OCT 图像语义分割任务，在 DeepLabV3+ 基线模型上提出了两个互补的注意力模块。TR 模块在编码器端通过双层路由注意力建立长距离依赖，解决细长结构断裂问题，使 mIoU 提升 3.9\%。SAE 模块在解码器端通过坐标注意力编码空间位置信息，提升边界分割精度，以 25M 参数量换取 5.9\% 的 mIoU 提升。两模块同时使用使 mIoU 达到 0.911（+7.1\%），目标类别 IoU 达 0.826（+16.3\%），HD95 降至 10.68（-12.6\%）。相比 TransUNet，本文方法在 mIoU 高 12.5\% 的同时内存占用低 53.3\%。
